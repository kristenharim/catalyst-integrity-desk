# Bob log

Running record of what IBM Bob built versus what was done by hand. The README's Bob
section gets written from this file, not from memory.

Rule: every entry names the tool. If a line says "by hand", it is not Bob's work and must
not be described as Bob's work in the README.

Corrected 2026-07-21. Three rows read "Claude Code" for work Bob did: the redline loop
and both console tasks. Bob writes its own row and had been naming the wrong tool, which
understated it on the one requirement the submission cannot lose. Checked against the task
exports, the commit messages, and the changed-file list of the session before changing them.
`AGENTS.md` now tells Bob what it is called, so the next row should not need this.

## Build

| Date | Tool | What | Transcript |
|---|---|---|---|
| 2026-07-21 | by hand (pre-Bob) | `engine/runway.py`, `engine/ctgov_history.py`, `engine/gap.py`. Phase 1, verified against live SEC and ClinicalTrials.gov. | n/a |
| 2026-07-21 | by hand (pre-Bob) | `HANDOFF.md`, `docs/SPEC.md`, `docs/FINDINGS.md` 1.1-1.8 and section 2, `docs/PORT.md`, `docs/DEMO.md`, `docs/BOB_PROMPTS.md`. | n/a |
| 2026-07-21 | by hand (pre-Bob) | `AGENTS.md` and `.bob/custom_modes.yaml`. Bob reads AGENTS.md by default and this repo had none, so it would have read CLAUDE.md, which is addressed to a different tool. The modes put a fileRegex on edit permission so the three verified engine modules cannot be rewritten. | n/a |
| 2026-07-21 | IBM Bob | Prompt 1, the governance port. Copied `ledger.py`, `challenge.py`, `classifier.py`, `granite.py` from the risk desk, rewrote the two prompt strings for catalyst vocabulary, kept `_fabricated()` byte for byte. Wrote `engine/contract.py`, the flattener from `CatalystContract` to the packet `scan_breaches` reads, with a round-trip demo. | `docs/bob-sessions/phase2-governance-port.json` |
| 2026-07-22 | IBM Bob | Prompt 2, the redline loop. Wrote `orchestrator/redline.py`: `as_directions()` translates before/after metric packets into word-only direction labels (the `scenario.py` pattern); `ContractDelta` pairs approved and recomputed contracts; `run_redline()` detects a gap breach and calls `build_challenge`. Extended `granite.py` SYSTEM_PROMPT with metric definitions (gap_months sign, max_days_expired meaning) so a live call cannot misread negative gap as a late trial; extended `_user_prompt` to surface the directions block. Wrote `tests/test_redline_fabrication.py`: fabrication-guard test (fake transport injects invented figure, stub fallback fires), scripted-amendment test (synthetic packets, offline), live Granite test (asserts no invented figure in returned rationale). All three pass. | `docs/bob-sessions/phase2-redline-loop.json` |
| 2026-07-22 | IBM Bob | Console planning pass. Read `docs/BOB_PROMPTS.md` Prompt 3 and `docs/DEMO.md`, then wrote `docs/plans/console.md`, the plan the two console build rows below execute. Carried one layout constraint stated as outranking everything else: the demo opens on the Rocket revision timeline rather than the contract list, with the 677-day row visible without scrolling or a click and marked distinctly from ordinary revisions. Logged 2026-07-21, late: the row was missing entirely until the transcript was exported and did not match any existing row. | `docs/bob-sessions/phase3-console-plan.json` |
| 2026-07-22 | IBM Bob | Console sub-task 1. Wrote `console/make_snapshot.py`: serialises SANA/PRME/RCKT engine output to `data/snapshot.json` (runway fields + provenance, trial dict, `TrialHistory.as_dict()` with `svg_x` positions computed on a 1100px canvas), scripted +9-month RCKT amendment, Granite redline with 5-attempt exponential backoff (30s base, doubles). Asserts `source == "granite"`, exits non-zero on stub or missing credentials. `--verify` flag prints gap_months, revision count, and source. Removed `data/snapshot.json` from `.gitignore` (demo artifact, committed so judges can run without credentials). | `docs/bob-sessions/phase3-console.json` |
| 2026-07-22 | IBM Bob | Console sub-tasks 2–6. `requirements.txt` (flask>=3.0). `console/app.py`: loads snapshot at startup, four GET routes + POST /redline/decide + GET /redline/confirm, no arithmetic in handlers. Templates: `base.html` (dark monospace layout, nav), `contracts.html` (ranked reliable rows + flagged section), `detail.html` (SVG revision timeline with carried-expired node in red at cy=72, gap calculation table with XBRL tag sources), `redline.html` (classification badge, breach table, Granite memo, Accept/Reject form), `confirm.html` (verdict + ledger verify badge + tamper-demo blockquote). `tests/test_console.py`: 9 tests — routing, 677 marker, carried-expired CSS class, granite/no-stub in memo, number-provenance parametrized over both routes. Provenance failure was verified (9999 injection named the token and the route). Display values pre-formatted in snapshot (`display` sub-dict + `gap_months_1f`) so every rendered number is a verbatim substring of the JSON. | `docs/bob-sessions/phase3-console.json` |
| 2026-07-22 | IBM Bob | Prompt 3 repair: the integrity badge. Fixed `GET /redline/confirm` to call `BeliefLedger.verify()` at render time instead of reading `?intact=` from the URL. Added `test_badge_flips_on_tampered_ledger`: POSTs a decision, mutates a byte inside the card payload (not the verdict word, which is in review_log.jsonl and not hashed), asserts the badge flips; seen failing before the fix. Added `--displays` flag to `make_snapshot.py` via `apply_displays()`: loads snapshot, recomputes all display sub-dicts from raw numerics, writes back; idempotent. Used it to add `breach.display` strings to the existing snapshot. Replaced all `%.1f` filters in `redline.html` with `display.*` strings. Added `/redline` to the provenance parametrize (was absent, passing only because the snapshot happened to contain those digit strings). `PORT` env var on `app.run`, defaulting 8050. | `docs/bob-sessions/phase3-repair-and-widening.json` |
| 2026-07-22 | IBM Bob | Prompt 3 widening: real ranked list and real flagged row. Widened `TICKERS` to `["SANA","PRME","RCKT","BEAM","SRPT"]`. Rebuilt `data/snapshot.json` via live Granite call: SANA drops (no pivotal trial), PRME/RCKT/BEAM rank reliably, SRPT appears in the flagged section (`reliable=False`, note: "cash-positive operating quarter(s) in TTM window ... likely a one-time inflow"). `apply_displays` covered new tickers automatically. Added `test_contracts_flagged_section_contains_srpt` and `test_srpt_has_no_rank_number`: the latter parses `<tr>` cells and asserts no plain-digit rank cell appears in SRPT's row. 15 tests pass (1 skipped without credentials). | `docs/bob-sessions/phase3-repair-and-widening.json` |
| 2026-07-21 | IBM Bob | Prompt 3, the breach moment: language, consequence, and the decision receipt. Same Bob session as the row below, split into two log rows. Serialised `lapsed` and `lapsed_history` per contract in `make_snapshot.py` (same `svg_x` treatment as binding history); updated the 677-day assertion to search both binding and lapsed histories; added `_cmd_bar()` to precompute monitored/active-breaches/lapsed-expectations counts; `apply_displays` populates `cmd_bar`. Rebuilt `data/snapshot.json` via live Granite: RCKT now binds to NCT06092034 (2028-04, gap -14.5 months "financing required") with NCT04248439 on `lapsed`. Added `test_snapshot_no_lapsed_catalyst` (the check that catches a stale snapshot; seen failing on the old file). Templates: renamed all "Catalyst date" labels to "Registered primary completion"; removed `&mdash;` from visible text; added lapsed-trial timeline section to `detail.html` with a dark red SVG background; added consequence sentence built from snapshot fields on `/contract/RCKT`; restructured `/redline` as three-column story (registered expectation / amendment / thesis status) plus Integrity memo card (Granite accent) and analyst-claim redline; replaced confirm badge with full decision receipt (author, timestamp, card id, what changed, thesis state, prev hash, entry hash, chain diagram). Added command bar to `base.html` via context processor (counts from snapshot, no Jinja arithmetic). Colour discipline: emerald = within band, red = integrity failure only, electric blue = Granite. Updated `app.py` to pass receipt dict via URL param to confirm; confirmed `python3 console/app.py` now works via `sys.path.insert`. 17 tests pass. | `docs/bob-sessions/phase3-real-breach.json` |
| 2026-07-22 | IBM Bob | Prompt 3, the real breach: delete the scripted amendment. Replaced `_build_rckt_redline`'s scripted +9-month shift with the real lapse event. Approved contract built from `rckt.lapsed[0]` (NCT04248439, pcd 2026-05-05, gap +8.4 months). Recomputed contract is the real current contract (NCT06092034, 2028-04, gap -14.5 months). `BeliefCard.scope` changed from `"company:RCKT"` to the NCT id. Serialised `prior_trial`, `prior_pcd`, `prior_gap_months`, `current_trial`, `current_pcd`, `current_gap_months`, `no_amendment_filed` into the redline block; `apply_displays` adds `lapse_display.prior_gap_1f` and `current_gap_1f`. Removed unused `deepcopy`, `timedelta`, `RCKT_SHIFT_MONTHS`. Rewrote `redline.html`: left column shows thesis as approved against NCT04248439; centre column states "No amendment was filed. The registered completion date passed." with struck-through NCT04248439 and the new binding trial; right column flips gap from +8.4 (struck through) to -14.5 in red. Fixed `--` in all template headings to `&middot;` (`RCKT -- ROCKET PHARMACEUTICALS`, `carried expired -- 677 days`, `RCKT -- LAPSED`). Rebuilt snapshot via live Granite (first attempt). All 17 tests pass. | `docs/bob-sessions/phase3-real-breach.json` |
| 2026-07-22 | IBM Bob | Prompt 3, the anchor: make deletion detectable. Added `orchestrator/anchor.py`: `record(ledger)` writes head hash and entry count to `data/ledger.anchor` after each successful approve; `check(ledger)` returns one of three strings: `"intact"` (chain verifies and matches anchor), `"tampered"` (verify() fails), `"truncated"` (chain verifies but head/count disagrees with anchor -- covers deletion, truncation, wholesale replacement). Wired `record()` into `redline_decide` after `apply_decision`; switched `redline_confirm` from `ledger.verify()` to `anchor_check()`. Updated `confirm.html` badge to three states with distinct styling: emerald intact, red tampered, amber truncated-or-replaced. Updated the tamper demo blockquote to describe all three. Three new tests in `test_console.py`, each seen failing before the fix: delete-newest-entry (truncated), replace-whole-chain (truncated), byte-edit (tampered-not-truncated). Updated `test_badge_flips_on_tampered_ledger` to use badge HTML entity chars instead of colour hex (hex appears in CSS regardless of state). No wording in the UI says "immutable" or "append-only". 19 tests, 1 skipped, three engine gates pass. | `docs/bob-sessions/phase3-ledger-anchor.json` |
| 2026-07-22 | IBM Bob | Prompt 3, the receipt: read it from the ledger, not the URL. `redline_decide` previously redirected with a `receipt=` JSON blob in the URL; a forged URL rendered `nobody:forged` and `deadbeef` hashes beside an intact badge. Fixed: redirect carries `verdict` only; `redline_confirm` builds the receipt from `ledger._last()` at render time -- author, timestamp, card id, what changed, thesis state, prev hash, entry hash all come from the stored entry. Added `test_confirm_ignores_forged_receipt`: POSTs a real approve, requests confirm with a forged `receipt=` param, asserts the page shows the real author and real hash and does not contain the forged values; seen failing before the fix. Added `data/ledger.anchor` to `.gitignore`. 21 tests pass. | `docs/bob-sessions/phase3-receipt-from-ledger.json` |

| 2026-07-22 | IBM Bob | Prompt 4 reduced: the panel. Wrote `research/panel.py`. For the 12 tickers in `engine/runway.py`'s demo list, builds each `CatalystContract` (7 of 12 resolved; 5 have no live pivotal trial matched by sponsor name), expands to one CSV row per PCD revision across the binding catalyst and all lapsed trials, emits `research/panel.csv` (33 rows, 10 trials, 24 columns). Reversal filter (finding 1.7): +X then -X within 60 days tagged `is_reversal=True`. Three carried-expired revisions present: RCKT NCT06092034 30d, RCKT NCT04248439 677d, ARVN NCT05654623 2d. Queue sorted by active breach then max_days_expired then gap ascending. Figure `research/figures/revision_vs_runway.png`: scatter of runway_low_mo vs moved_days, ordinary revisions in teal, carried-expired in red squares with ticker+days annotations, dark background matching the console palette. Caption states "12 companies, not the sector", sample size, "Descriptive only. No causal claim." 21 tests pass; three engine gates pass. | `docs/bob-sessions/phase4-panel.json` |

| 2026-07-21 | by hand (Claude Code) | Docs only, no product code. Corrected the test counts in `README.md`, which claimed "16 passed, 1 skipped" in the quickstart, "17 passed" with credentials, and "14 passed" / "13 passed, 1 skipped" in the verification section. Measured actual is 21 passed and 1 skipped, 22 passed with watsonx credentials, the single skip being `test_no_fabrication_live`. `docs/SUBMISSION.md` said "22 tests" without the split and now states both. Marked `HANDOFF.md` superseded at the top: it describes the pre-Bob repo and calls the governance port, redline loop, console and panel unbuilt, all of which now exist, and it was still the first document `CLAUDE.md` sent every session to. `CLAUDE.md` now points at `README.md` and `docs/STATUS.md` instead. Dated entries in `docs/STATUS.md` and the Bob rows above were left alone: their counts were correct when written and a log is not corrected by rewriting it. | n/a |

| 2026-07-21 | by hand (Claude Code) | Docs only, no product code. Published the repo public at https://github.com/kristenharim/catalyst-integrity-desk after a credential scan across all branches, and filled the link into `docs/SUBMISSION.md`, `docs/STATUS.md` and the README quickstart. Verified the judge's path rather than assuming it: cloned the pushed repo into a clean directory, ran the suite there (21 passed, 1 skipped, same as local), and requested `/`, `/contracts` and `/redline` through the Flask test client with the three `WATSONX_*` variables unset, all 200 and the 677-day row present on the redirect target. So the "clone, install Flask, run, no credentials" claim in the README is tested from a clone and not just from the working tree. | n/a |

| 2026-07-21 | by hand (Claude Code) | Docs only, no product code. Filed `phase4-panel.json`, closing the panel row. Two files were exported; the other was byte-identical to `phase3-repair-and-widening.json` once `exportedAt` was stripped, same 107 messages and same 780,473 bytes, so it was discarded rather than committed as a ninth transcript. Identified the real one by content rather than filename: 46 hits on `is_reversal` and 27 on `revision_vs_runway`, neither of which existed before the panel task. Corrected the README and the submission pack, which claimed every Bob row was backed by an export and that there were seven transcripts at 4.4 MB. Actual is eight transcripts at 5.4 MB backing ten of eleven rows, two transcripts covering two rows each. The breach-moment row stays `pending export`. | n/a |

| 2026-07-21 | by hand (Claude Code) | **Product code, not docs.** Fixed the silent-drop defect on `/contracts`. `console/make_snapshot.py` dropped any ticker whose `build()` returned `None` with only a line on stderr, so the page showed four of the five tickers in `TICKERS` and nothing said the fifth had been requested. SANA has a reliable runway and zero trials matching the sponsor string `Sana Biotechnology, Inc.`, so it vanished. This is the exact shape of the failure the project's own rule forbids: unreliable rows are shown, never silently dropped. Added `_unresolved_row()`, which re-derives the reason rather than changing `build()`'s signature, so `engine/gap.py` is untouched, and distinguishes the two cases `None` conflates: no trial matched, versus every registered pivotal completion has lapsed. The second is the stronger date-integrity signal and was the more serious thing to be hiding. Added a `--unresolved` refresh flag on the `--displays` pattern so the block could be populated without a full rebuild, which would have needed watsonx credentials and regenerated the Granite classification. Verified the contracts, redline and cmd_bar blocks are byte-identical before and after, `classification.source` still `granite`, and the flag idempotent. New `unresolved` section in `contracts.html`, plus `test_unresolved_ticker_is_shown_not_dropped` and `test_every_monitored_ticker_appears_on_contracts`, the second being the general form so a sixth ticker cannot reintroduce it. Both seen failing against two separate mutations: template section removed, and `app.py` passing an empty list. 23 passed, 1 skipped. Three engine gates pass. Also fixed `.claude/launch.json`, which named port 5000 (taken by macOS ControlCenter) and ran the app by path rather than as a module. | n/a |

| 2026-07-21 | by hand (Claude Code) | **Product code, not Bob's.** Two features. First, the derivation table and thesis timeline: `_thesis_timeline()` and `_derivation()` in `make_snapshot.py` build a per-contract row list walking every step from filed data to the funding gap, each row naming the record it came from, rendered by the new `console/templates/_derivation.html` partial on both `/contract/<ticker>` and `/redline`, with the template doing no arithmetic. `_months_after()` and `_gap_months_between()` carry the date maths. The snapshot now pins its own `as_of` so a rebuilt file cannot silently reinterpret a stored date. Second, analyst belief entry: `GET`/`POST /belief/new` with `belief_new.html`, `_form_errors()` validating the form as an untrusted boundary (ticker must be monitored in the snapshot, NCT must match the registry format, thesis must be long enough to be a real rationale, gap must parse as a number) and `_card_from_form()` folding the invalidation conditions into `claim`, which is the text Granite is given, so a challenge is judged against the conditions the analyst said would change their mind. Monitoring starts from a written belief, so there has to be a way for a human to write one. 15 new tests covering timeline agreement with the contract, derivation sourcing, the review path writing nothing, ledger append, duplicate refusal, and bad input. 38 passed, 1 skipped. Three engine gates pass and the snapshot classification is still live Granite. Four mutations watched failing before the new checks were trusted: the timeline's gap set to `-99.9` (three tests failed), a `record` field emptied on a row claiming a tag, and a literal `8888` planted twice, once in the derivation partial and once in an SVG label, the provenance test naming both. One earlier mutation was a bad test rather than a bad check and is recorded because it was nearly mistaken for a hole: a literal added to the partial's `verdict-ok` branch, which never renders for a negative gap, so nothing was ever drawn. Also fixed a real defect found while doing this: `refresh_unresolved()` wrote the snapshot without going through `apply_displays()`, so a `--unresolved` run silently deleted every derived block from the file it rewrote and left something that still looked like a valid snapshot. Every writer now goes through the one function. Two strings were corrected after `docs/LIMITS.md` was written and made the overclaim visible: the confirm button said "start monitoring" and the receipt said "is now monitored", when the redline loop runs off the committed snapshot and does not read the ledger's live cards, so a belief written through the form is recorded and not yet watched. `docs/LIMITS.md` gains three sections at the strength the evidence supports: the pinned `as_of` closes half of the wall-clock item and the rebuild still reclassifies, the derivation names a record that nothing resolves, and the timeline's geometry is asserted only to fall inside its axis, so overlapping markers draw badly with every test green. | n/a |

| 2026-07-21 | by hand (Claude Code) | Docs plus one guard. Put `/belief/new` on the demo path: a 15-second beat at 2:05 in `docs/DEMO.md` answering the question the redline begs, which is what the thesis is being judged against, with the human gate retimed to 2:20 and the close to 2:50. Listed it as the first cut in the run-long order, because it is the only beat where nothing is wrong on screen and the 3:00 ceiling is a hard rule. Added it to the README tour, and noted the unresolved section there too. Confirmed the form writes to `data/decisions.jsonl`, already covered by the reset, and added `data/ledger.anchor` to the README's reset command, which had it missing while `docs/DEMO.md` had it. Fixed the test counts again, stale in five places after the derivation work landed, then wrote `test_readme_test_count_matches_reality` so this stops recurring: it collects the suite and asserts the README's stated figure matches. The count had already shipped wrong twice, at 16 and at 21, which is a checkable number being wrong in the first thing a judge reads, in a project whose whole claim is that its numbers are checkable. Seen failing on the exact regression, README reverted to 21. Also fixed the stale "three views" in the README and the layout block. 39 passed, 1 skipped. | n/a |

| 2026-07-21 | by hand (Claude Code) | Refinements to the belief form and the derivation drawer, plus the limit they closed. Logged first as "by hand (Kristen)" by the session that committed it, which was reading a working tree it had not written; corrected because the rule in `CLAUDE.md` is that a tool logs what it wrote, and a human getting credit for a tool's work is the same failure as the reverse. `belief_new.html` stopped claiming too much: the confirmation read "is now monitored", which was false, because the redline loop runs off the committed snapshot and a new card is picked up at the next rebuild rather than immediately. It now says recorded and hash-chained, with the delay stated on screen. `redline.html` reworked the provenance drawer that opens the headline figure onto every step behind it, native `<details>`, no script. `docs/LIMITS.md` marked the wall-clock limit half closed rather than closed: the snapshot pins its own `as_of`, written once and never re-read from the clock, and the timeline's "today" marker reads that pinned field, so a committed snapshot cannot silently reinterpret itself. | n/a |

| 2026-07-21 | by hand (Claude Code) | **Product code, not Bob's.** The monitoring queue, review item 4. `_queue()` and `_queue_rows()` in `make_snapshot.py` build one row per contract per reason to look, sorted worst first, with per-state counts precomputed so the template uses no `|length` and no comparison. `GET /queue` in `app.py` slices it; `console/templates/queue.html` renders it; nav link added. A contract appears more than once when more than one thing is wrong: Rocket is both breached and carrying a lapsed registered expectation, and collapsing that to a single worst state hides one of them. Two states the review asked for are absent because one snapshot cannot compute them, a changed SEC tag path and a date that moved since the last look, and the page says so rather than showing them empty. Two things were wrong on first render and were found by looking at the page, not by a test. Rows with an unreliable burn printed a gap of `2.6 mo` beside "burn estimate unreliable", which ranks the row in the reader's head even though no column calls it a rank, and the existing no-rank test passes on it because that test looks for a plain-integer cell on `/contracts`; the queue now prints "not rankable" and has its own test. The first state label read "newly breached", a comparison against a previous snapshot that does not exist; renamed to "breached", with a test rejecting `newly`, `since last` and `moved since` in any label. Seven mutations watched failing before the checks were trusted: a clear row flipped to breached, an unreliable row dropped, Rocket's lapsed row dropped, the queue re-sorted so the clear row leads, a whole contract removed, a gap figure restored to an unreliable row, and "newly" put back. Also widened `test_readme_test_count_matches_reality`, which checked only the "N passed, 1 skipped" figure and so let the with-credentials figure go stale in the same commit that fixed the other one; it now checks both figures in both `README.md` and `docs/SUBMISSION.md`, and fails if its own patterns stop matching anything. Four mutations on the widened guard, including the case where it silently stops guarding. 49 passed, 1 skipped. Three engine gates pass. `docs/LIMITS.md` gains the queue section, including the six-month "approaching" threshold being a judgement that nothing validates. | n/a |

| 2026-07-21 | by hand (Claude Code) | **Product code, plus a design doc.** Two hollow checks, both found by mutating them rather than by reading them. First: `test_srpt_has_no_rank_number` guards the rule that unreliable rows are never ranked, but checks one page for one shape of violation, a plain-integer rank cell on `/contracts`. Adding a gap column to the flagged table still passes it, confirmed by running that mutation. Replaced the coverage with `test_unrankable_rows_carry_no_gap_figure`, parametrised over `/contracts` and `/queue`, which parses every `<tr>` and asserts no row naming an unreliable ticker contains that contract's gap string; the narrow test is kept as defence in depth. Second: the number-provenance test ran over four routes and every one of them renders a negative gap, so the `verdict-ok` branch of `_derivation.html` was never on a tested page. A literal planted there earlier in the session went undetected and was briefly logged as a bad mutation rather than a real gap; it was a real gap. Added `/contract/BEAM` to the parametrize, BEAM being the only reliable contract with a positive gap, and the same planted `8888` now fails naming the route. 52 passed, 1 skipped. Also wrote `docs/WORKSPACE.md`, the ticker-first workspace design: the demo/workspace split over one shared engine, the discovery pipeline mapped onto the functions that already implement it, the two human gates (which trial underwrites the thesis, and approving the proposed contract), document intake that extracts assumptions and never figures, and the nightly diff that makes the two queue states `docs/LIMITS.md` rules out today computable. Marked unbuilt throughout, with a section on what it does not solve: sponsor-to-issuer identity as the gating problem, the wall clock in `build()`, no compare-and-swap on the ledger head, the self-owned anchor, unproven byte reproducibility, and API rate limits. Cross-linked from `docs/CONJECTURE.md` as Rung 0. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Product code, not Bob's. The foundation for workspace mode, and a reframe.** Ran a three-model council (Claude, Cursor, Codex) on whether the project could be pitched as answering "does this technology work". Verdict, and it was unanimous on the key point: it cannot, because a model making that judgement is exactly the thing this architecture forbids, and the word "credibility" is a feasibility claim wearing a synonym. Kept the target, changed the claim: the system measures whether public, dated, self-authored commitments were kept, revised, superseded or allowed to expire without reconciliation, and does not measure whether the underlying claim was ever true. `docs/PRINCIPLE.md` states it and lists what follows. Three things built. (1) The seam, `evidence/`: an immutable content-addressed `EvidenceSnapshot`, a `SnapshotProvider` interface, `FrozenSnapshotProvider` (committed artifact, no network) and `LiveSnapshotProvider` (fetch then freeze into the identical schema, so a live run becomes tomorrow's fixture). Negative results are first-class, because a source queried and empty is a different fact from a source never queried and both render as [] if you store only what you found. Two timestamps, never one. `tests/test_layering.py` walks the AST and asserts `engine/` and `orchestrator/` cannot import `evidence`, so no module that computes a displayed number can ask whether it is running live and answer differently. (2) `engine/promise.py`, the highest-risk item the council named: promise identity. Two dates are only comparable if they describe the same commitment, so every revision is classified as unchanged, date_only, scope_revision, supersession or uncertain, and only the first two may produce a slip number. A changed endpoint, a changed population, a terminated status or an unreadable dimension returns no number at all. The failure this prevents is worse than an invented digit: an exact subtraction across two different commitments, rendered with a full provenance trail that makes it look more authoritative rather than less. 18 tests, most of them asserting a refusal. (3) `orchestrator/lexicon.py`, the forbidden-claims list made executable. Granite's rationale passes through it at runtime beside `_fabricated()`, so a model saying "this looks like fraud" is discarded exactly as one inventing a figure is; every rendered page and every claim-bearing doc is scanned; an exemption marker covers prose that quotes a banned phrase in order to forbid it, and a test asserts the marker only ever lands on a line that is doing the forbidding. It caught three violations already shipped, including "What makes it credible" as a heading in the submission pack. Five mutations watched failing: a compute module importing across the seam, a feasibility claim on a rendered page, the banned word in the README, the runtime guard deleted from `granite.py`, and the exemption marker used to silence an assertion. 99 passed, 1 skipped. Five engine gates pass. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Product code, not Bob's. Workspace mode, and an audit that corrected this project's own headline numbers.** Built on the seam logged in the row above. `engine/dimensions.py` reads the primary outcome, enrolment and phase per registry version out of `data/cache/`, which `fetch_history` already downloads in full and never reads past the status module; new file, so the three verified engine modules and their interfaces are untouched. Feeding those into `engine/promise.py` answered the question nothing had asked: is a date movement across two versions actually one commitment slipping. Five of seven trials in the snapshot say no. `NCT04248439` reports 1,008 days, of which a single +1,430-day revision coincided with the primary endpoint changing from "Phenotypic correction of bone marrow colony forming units after infusion of RP-L102" to "Bone Marrow Colony-Forming Cell Mitomycin-C resistance"; the supported figure is -422 days across the two revisions where the commitment held. `NCT06092034` reports 943 and supports 0: its one stored revision changed both endpoint and enrolment (12 to 14). Unaffected and stated as such in three places so the finding is not read as bigger than it is: the 677-day expired-date result is one version carrying an already-passed date, not a diff across two commitments, and the funding gap compares the current registered date to the runway. Added `--transitions` to `make_snapshot.py` (offline, reads only the cache, idempotent) which classifies every stored revision and writes the audit into the committed snapshot, because the cache is gitignored and a figure that changes depending on whether a local cache exists is not a figure this project may show. Both the reported and the established totals now render wherever either did, non-comparable revisions carry an amber ring on the timeline, `docs/LIMITS.md` gains the table, and the README's "What it found" section corrects itself in place rather than quietly dropping the old numbers. Then workspace mode: `console/make_evidence.py` converts the committed snapshot into `EvidenceSnapshot` bundles so the flow runs with no credentials; `console/intake.py` resolves identity, ranks candidates with what their history does and does not support, and assembles a proposed contract from fields rather than generating one; four routes and `workspace.html` for ticker, discover, review and approve. Two gates are human because neither is inferrable from a ticker: which trial underwrites the thesis, and whether the proposal is right. The page shows the sponsor-to-issuer join and the queries that came back empty BEFORE any candidate, because if the wrong company's trials came back nothing below that line means anything. A lapsed date is listed and refused at the intake layer rather than merely omitted from the template, so a hand-posted form cannot bypass it. Unavailable monitors are listed as unavailable, since offering a condition the desk cannot evaluate tells the analyst it is being watched. Docs reframed around the kept-promise claim per the council verdict, with `docs/PRINCIPLE.md` as the statement of it. Four workspace mutations watched failing: a lapsed trial made selectable, negative results hidden, identity moved below the candidates, and the analyst's wording silently replaced by the proposal. One real bug found and fixed in my own test: `test_evidence.py` called `importlib.reload`, which rebound `provider.Incomplete` to a new class object while `console.app` held the old one, so `except Incomplete` stopped matching and a 404 route returned a 500 -- it passed alone and failed in the full suite, the signature of a test mutating global state. 119 passed, 1 skipped. Six module gates pass. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Research, and a negative result reported as one.** Asked whether the system would have flagged three named historical failures using period-accurate information. Checked rather than argued: queried ClinicalTrials.gov and found Theranos has exactly one record mentioning it, lead sponsor Stanford University, so Theranos never registered a trial as sponsor; "Nikola" returns five studies, all physicians of that name. Neither is in scope, and neither is a thin AI application layer, because none of them ever made a dated versioned self-authored commitment to a registry that keeps versions. Wrote `docs/BACKTEST.md` stating that as a No with the queries attached, plus the eight-point admission test a domain must pass, and named the one Theranos signal that IS in scope and is not built: conspicuous absence, a diagnostics company registering nothing while its peers register trials. Built `research/backtest.py`, which is the part that can be verified: period-accurate replay against a cutoff date, discarding every registry version submitted after it. Honest rather than simulated because ClinicalTrials.gov keeps every version with its submission date. Re-derived the headline 677 days from raw versions by a different route than the engine uses -- the date expired 2022-06-01, the sponsor corrected it 2024-04-08 -- and established that the signal was publicly observable from 2022-06-02, the day after it passed, on a day when nothing was filed and nothing happened. `first_observable` samples the day after each registered completion and not only filing dates, because sampling filings finds the first day someone touched the record, a later and flattering number. Scanned all cached trials: six of fourteen carried an already-passed date across fourteen stretches, and NCT05654623 left the same dead date standing through four successive filings at 40, 150, 349 and 392 days. Detection latency is the claim; prediction is not, and a test scans every replay output through the lexicon so an observation cannot quietly become a forecast. Three mutations watched failing: the future leaking into the replay by flipping a comparison, `first_observable` sampling only filing dates (2023-08-01 instead of 2022-06-02), and a planted causal phrase in an observation. 131 passed, 1 skipped. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Product code. The audit audited, and the previous row's finding corrected.** Kristen asked what the failure mode of the fix was. It had two, pointing in opposite directions, and the second is worse. `classify()` treated ANY endpoint difference as a scope revision, but an endpoint is free prose and an exact string comparison cannot tell a reword from a redefinition. On NCT04248439 the endpoint went from "Phenotypic correction of bone marrow colony forming units after infusion of RP-L102" to "Bone Marrow (BM) Colony-Forming Cell (CFC) Mitomycin-C (MMC) resistance" -- and in Fanconi anaemia, MMC resistance of BM colony-forming cells is HOW phenotypic correction is measured, so those may be one endpoint named more precisely. First failure: a real 1,430-day movement excluded because the sponsor reworded at the same time. Second and worse: a sponsor wanting a delay gone from the comparable total need only reword the endpoint, and the guard obliges. A guard the subject can defeat by editing prose, in the direction that flatters them, is not a guard. Measured the exposure before fixing it: of 29 substantive version-pair transitions across the cache, 7 are driven by a free-text reword alone. Fixed by splitting dimensions into structured (counts and enumerations: enrolment, phase, milestone, actor, subject) and free text (endpoint), adding a `text_revised` state, and replacing the two-number total with `slip_breakdown()` returning established, contingent, upper_bound and refused. Contingent days are reported in their own total, never folded into established (which would restate the original bug) and never dropped (which is the laundering vector). Result: NCT04248439 is contingent rather than refused and its upper bound is exactly the reported 1,008 days, so the previous row's "-422 is the supported figure" was an OVERCORRECTION and is now retracted in LIMITS.md, README and SUBMISSION. NCT06092034 stands refused, because its enrolment moved 12 to 14 and no reading of any text rescues a count. Timeline nodes now distinguish amber "wording only" from red "not comparable". Also added the denominator caveat to BACKTEST.md: fourteen is the number of trials that happen to be cached, a convenience sample of the companies picked to illustrate the problem, so "six of fourteen" is a direction and not a base rate, and the honest form is "in the fourteen trials examined". Three mutations watched failing: folding TEXT_REVISED back into COMPARABLE, dropping contingent_days, and moving endpoint into the structured bucket. 137 passed, 1 skipped. Four module gates pass. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Research plus product code. The first random cohort, and it contradicts the headline.** Built `research/cohort.py`: a seeded, stratified, resumable draw from a stated frame (interventional, phase 2/2-3/3, registered primary completion, first posted 2016-01-01 to 2023-12-31, by lead-sponsor class), measured with the product's own `fetch_history`, `from_cache` and `slip_breakdown` rather than a second implementation. 85 of 240 drawn measured so far, fetch-bound, skewed to INDUSTRY because NIH trials carry far more versions. Two results contradict things this repo already says, so they are reported at n=85 rather than held until the run finishes. FIRST: 80.6% of industry trials in the sample carried an already-passed registered completion date at some point, median stretch 246 days, p90 1018. Rocket's 677 days sits near the 75th percentile, not at the tail. Carrying a dead date is ORDINARY. The README opened on 677 as though it were remarkable and now says so plainly; the claim gets bigger rather than smaller, because the finding is not one sponsor lapsing but that nobody reconciles any of it across an entire registry as a matter of course. SECOND: the contingency rate is 6.7% (11 of 164), not the "roughly a quarter" I reported from the convenience sample, which overstated it about 3.5x. That materially changes the adjudication workload from "needs staffing" to "occasional task". Named a defect in the cohort report rather than fixing it: "refused" bundles genuine scope revisions with continuity that could not be established, which are a finding and a data gap respectively, so 39.6% is an upper bound and not a measurement. Also shipped `orchestrator/adjudication.py`, the contingency-resolution workflow: contingent transitions posed as a Y/N question with both endpoint texts side by side and the consequence of each answer stated before it is given, rulings stored as labelled ground truth with an author, and `apply_rulings` returning the adjusted total always beside the unresolved count so a human-resolved figure never reads as a machine-established one. Expert time is rationed by skipping rewords that moved no date. Caught a real bug in my own module doing it: `apply_rulings` counted zero-movement rewords as unresolved while `questions_from` skipped them, so the queue length and the waiting count disagreed; the self-check now asserts the two ration identically. Found and recorded a defect in a protected module: `_cached()` writes straight to its target path, so an interrupted fetch leaves a truncated file that breaks every later read of that trial, which is how one corrupt file of 335 took down five unrelated tests. Fixed the readers this project owns to skip unreadable entries and classify the version as unestablished; logged the atomic-rename fix in LIMITS.md rather than editing `engine/ctgov_history.py`. 137 passed, 1 skipped. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Research plus product code. The refusal bundle resolved, the innocence check done, and the first product-shaped artifact.** Recorded the refusal reason per transition in `research/cohort.py` and re-measured, prioritising the under-measured strata. Result on NIH (n=95, complete): of 295 transitions, 25.4% refused because a count or enumeration genuinely changed, 10.5% because the commitment was superseded, and **0.0% because anything was unreadable**. The bundle I flagged as uninterpretable was correct and merely unlabelled; the hole was empty, which is worth recording because a check that comes back clean is evidence too. INDUSTRY still shows "measured before the split" and its 40.0% stays an upper bound until re-measured. Stratification: NIH sponsors carry a dead date about as OFTEN as industry (82.1% vs 80.6%) and roughly two and a half times as LONG (median 619 days vs 246), so the strata are not poolable and every rate is now labelled by stratum rather than averaged. Innocence check on the 246-day median: pulled 42 CFR 11.64 from the eCFR versioner API, which sets a field-specific deadline, "Primary Completion Date must be updated not later than 30 calendar days after the clinical trial reaches its actual primary completion date". The median is roughly eight times that window and the p90 about thirty-four times, so annual update batching is ruled out as a complete explanation. Stated carefully: the rule concerns updating to ACTUAL, the observation is an ESTIMATED date left standing, and this data cannot say which happened for any given trial, so the window is used as a reference line on a distribution and no stretch is called a breach. Naming the duty is what the lexicon requires before any disclosure-adjacent statement is permitted at all. Built `research/sponsor_profile.py`, the first artifact shaped like the product rather than the investigation: one trial's dead-date stretches placed as a percentile against the cohort, with the regulatory window drawn as a reference line and the caveat rendered in the output. Rocket lands at the 84th percentile of 221 industry stretches. Its self-check asserts the output passes the lexicon and that an empty cohort yields NO percentile rather than a plausible-looking one. Wrote `docs/AMENDING_PROTECTED_MODULES.md`, the procedure the "do not touch" rule lacked: four conditions (demonstrated defect, no interface change, no behaviour change on correct input, all gates pass), what an amendment must carry, and what stays forbidden. The open case is the `_cached()` non-atomic write, which meets all four and is left for a human to approve. 137 passed, 1 skipped. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Correction to a figure published one turn earlier.** A background measurement pass finished while a manual merge was appending to the same append-only cohort store, so rows were duplicated and the reported n was inflated. Figures corrected 2026-07-22 by recomputing from the committed history rather than from memory: at this commit the store held **179 rows and 131 distinct trials, 48 counted twice**, and the "169 trials measured" published mid-run was a row count covering **123** distinct trials, an inflation of about 37%. This row originally said "49 of 180 rows were duplicates and the reported n=169 was really 131", which was wrong twice: off by one row, and pairing a row count from one moment with a distinct count from a later one. Every rate computed from it was slightly wrong and all of it in the flattering direction, because more rows reads as more data and never as a bug. Same shape as the ledger problem this project already solved and as the truncated cache entry logged above: a store that only grows gets read as though growth were evidence. Fixed in `load_results()` rather than by cleaning the file: deduplicate on read, last row wins, and a row carrying refusal reasons beats one predating them regardless of write order, so resuming a run cannot silently downgrade the schema of rows already measured. The report now prints distinct trials beside stored rows so the two are visible together. Three tests added, including one asserting the printed n equals the distinct count. Corrected numbers: INDUSTRY n=60, 80.0% carried a dead date (published as 80.6%), median 240 days (published as 246); NIH n=57, 80.7% (published as 82.1%), median 580 days (published as 619). Rocket moves from the 84th to the 85th percentile of 188 industry stretches. The substance is unchanged, the numbers were not, and README, COHORT.md and LIMITS.md all say so in place rather than quietly restating. Residual limit recorded: deduplication is on read, so the file still holds the duplicates, which is deliberate because discarding a measurement to tidy a count is the wrong direction, but any consumer bypassing `load_results()` still sees the inflated view. 139 passed, 1 skipped. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Product code, and the first amendment to a protected module.** Applied the `_cached()` atomic-write fix in `engine/ctgov_history.py` under `docs/AMENDING_PROTECTED_MODULES.md`, which had never been invoked before. Amendment approved by the owner. The four conditions, each checked rather than asserted. (1) **A defect, demonstrated:** beyond the `NCT03919071-v356.json` case already logged, the completed cohort held two NIH trials, `NCT04269902` and `NCT04071223`, that stored a `JSONDecodeError` in place of a measurement and silently cost that stratum two trials. (2) **No interface change:** same signature, same return, callers cannot tell. (3) **No behaviour change on correct input:** all four module gates captured before and after and diffed byte for byte, identical. (4) **Gates pass**, plus the new test. Fix is three lines, write to `path.<pid>.tmp` and `os.replace`, with the pid keeping two concurrent fetchers of the same version off each other's temp file, and a dated comment at the changed line stating the defect so the next reader knows it was argued rather than casually edited. Test named in this row: `tests/test_ctgov_cache.py::test_interrupted_cache_write_leaves_no_readable_entry`, watched failing against the pre-amendment module (the truncated file survives at the target path) and passing after. A second test pins condition 3, that a written entry still reads back from cache without refetching. Re-measured the two poisoned trials with the fix in place: both succeeded, 708 and 301 versions, and NIH closed from 58 to 60. `docs/LIMITS.md` and the procedure's own "open case" section updated in the same pass. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Product code. The cohort store closed, a snapshot id every published number cites, and the lexicon extended before the content it guards exists.** Finished the draw: 240 distinct trials, 60 in every stratum, zero failed to measure. Compacted `data/cohort/results.jsonl` to one row per trial with the 123 superseded rows appended to `results-archive.jsonl`, which closes the residual recorded last turn: deduplication on read fixed the readers this project owns and left the inflated view in the file for anyone else. Nothing deleted, because discarding a measurement to tidy a count is the wrong direction. Caught a real bug in my own compaction before it shipped: the first version selected winners by calling `load_results()`, which re-reads the file, then compared object identity against rows it had not loaded, matched nothing, and archived the entire store. It failed loudly ("0 rows kept") and the store was restored from the archive. Root cause was two copies of the dedup rule, so the rule is now `_dedupe()` and both `load_results()` and `compact()` call it. Added `--freeze`: a content-addressed snapshot id over the measured rows plus the frame, so a snapshot cannot be quietly re-cut under the same name, written to `data/cohort/snapshot.json` as `cohort-65fdf1f71b1d`. `freeze()` refuses to cut a snapshot over any trial that failed to measure or any stratum whose refusals predate the reason split, because a snapshot that quietly drops trials is the n-inflation bug wearing the other sign. Extracted `stats()` so the printed report and the frozen snapshot cannot drift. **Removed the pooled all-strata section from the report**, which was live in the code against a standing rule: NIH sponsors carry a dead date about as often as industry ones and roughly 2.4x as long, so a pooled rate describes no population that exists, and `stats()` now raises on any stratum name outside the four rather than returning a number. Ran the INDUSTRY refusal re-measure to completion, closing the last "measured before the split" bundle: of 126 transitions, 34.1% a genuine count or enumeration change, 5.6% superseded, and 0.0% unreadable, matching the NIH result that the hole was empty. Extended `orchestrator/lexicon.py` for the AI/absorption domain ahead of any such content: an `absorption` kind for predictions about a vendor's roadmap or what a capability will be worth later, and a `quantification` kind for waste figures, which are Fermi estimates and may be published only as a contingent figure with the assumption on the same line. That exception is a new `unless` field on `Rule`, deliberately distinct from `EXEMPT_MARKER`: the marker excuses a quotation, `unless` excuses a claim that has been brought inside the evidence by labelling it. Seven planted absorption violations and three planted magnitude violations, each watched being caught in `demo()`. Eleven new store tests, four of them mutation-verified by appending a duplicate row, making a module read the store directly, editing one figure inside the frozen snapshot, and dropping a live row the archive supersedes. All four caught. | n/a |

| 2026-07-22 | by hand (Claude Code) | **The write-up, and a three-seat council that blocked it on two measurement defects.** Drafted `docs/WRITEUP.md` from snapshot `cohort-65fdf1f71b1d` and added it to the lexicon's enforced claim docs, where it immediately caught two violations in its own first draft. Then convened the council: a hostile reviewer attacking every sentence for overclaim, a target reader applying the "so what" test, and a methods auditor recomputing every figure from the store. **All three blocked, and two of the objections are measurement problems rather than wording problems.** Both confirmed against the store before acting on either. FIRST: `carried_until_corrected()` emits a stretch per consecutive version pair where an already-passed date stood, so every stretch needs a later filing to close it, and a trial that lapses and then files nothing produces no stretch and counts as never having carried one. The published rate is really "carried a dead date and then filed again". This **inverts the cross-stratum ranking**: OTHER_GOV has the lowest published rate (53.3%) and the highest share carrying an unreconciled lapsed date right now (96.7%), because 28 of its 60 trials went quiet. SECOND: the write-up claimed "a trial contributes one stretch per episode", which is false. Stretches are nested prefixes of one lapse measured to successively later filings: `NCT02931474` has 97 versions, 3 date revisions and **91 stretches**, and is 18% of the NIH stratum's whole duration distribution. Since the row count tracks how often a sponsor files for unrelated reasons, the duration distribution is weighted toward frequent filers, and NIH files a median of 106 versions against industry's 9. The headline **2.4x** NIH/industry ratio falls to **1.5x** measured one observation per trial, and the industry median is 240 per stretch against 390 per trial. Neither convention is wrong; only one was labelled. Both recorded in `docs/LIMITS.md` and left unresolved, because the fix changes what the study's headline measures and that is the owner's call, not a defect to patch. The write-up is marked DO NOT PUBLISH, the retracted paragraph is retained inline with its correction rather than deleted, and README, SUBMISSION and COHORT.md all carry the caveat in place. The methods auditor also caught the correction history's own arithmetic being wrong: "n=169 was really 131, inflated by 29%" paired a row count from one moment with a distinct count from a later one. Recomputed from git history: 179 rows and 131 distinct at the fix commit with 48 counted twice, and the 169 rows published mid-run were 123 distinct, so the inflation was about 37%. A wrong number inside the section whose purpose is to show that arithmetic gets checked. Separately, found by trying to answer the reviewers: `ends_with_uncorrected_dead_date` computed `hist.__dict__.get("status")` and `TrialHistory` has no `status` field, so the "not COMPLETED" exclusion never fired on any trial. Nothing consumed the flag, so no published figure was affected, which is luck rather than design. Replaced with the date's own ESTIMATED/ACTUAL type, which is the right discriminator, though the store predates the fix and needs a re-measure before the field is used. 154 passed, 1 skipped. Four engine gates green. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Owner decisions applied: the primary metric changed, and a wrong figure I had published one revision earlier was caught and retracted.** Point prevalence became the primary frequency measure and per-trial longest carry the primary duration measure, with the stretch-based figures retained and labelled "lapsed and subsequently filed again". "2.4x" is removed as a headline everywhere and the no-pooling rule is re-justified on three independent differences: reconciliation behaviour, point prevalence, and filing frequency. **Correction to my own last row.** The point prevalence I reported to the owner (83.3% industry, 96.7% OTHER_GOV) was wrong. It read the date's ESTIMATED/ACTUAL type from `research.backtest._versions()`, which does not return that field, so the comparison was `None != "ACTUAL"`, always true, and every completed trial that had correctly recorded its actual completion date counted as carrying a lapsed one. The filter never fired. True figures are 8.3% and 45.0%. Exactly the defect pattern this repo documents: a check that silently does not apply is indistinguishable from one that passes, it produced the more dramatic number, it moved in the flattering direction, and it survived my own review. Added `pcd_type` to `_versions()` and a test asserting a past ACTUAL date does not count. A second bug in the same area: `measure()` read the type from `revs[-1]`, the last version where the date VALUE changed, so a later ESTIMATED-to-ACTUAL flip at the same date left it stale; it now reads the latest submitted version. Over-counted by 3 to 6 trials per stratum. **The rank inversion survived both corrections and is now a first-class result**: the two frequency measures order the four strata in exactly opposite order, and the ordering is filing frequency (median versions 2, 4, 9, 106). 20 of OTHER_GOV's 27 currently-expired trials are invisible to the stretch measure. **Ran the `held_days` separation the council's Shadbolt objection required**, which is the distinction the whole thesis turns on: a revision filed while the date is still in the future is forecasting and the record stays honest, one filed afterwards means the registry carried a commitment its author had stopped believing. Industry: 66 of 126 dated revisions (52.4%) were filed after a lapse, and 43 of the 52 trials that revised a date at all let at least one lapse first (82.7%). NIH is the exception at 31.5%. So the finding is not a restatement of the delay literature, and this is now the headline. The owner's return trigger (mostly pre-revision, thesis resize) did not fire. Re-measured all 240 trials twice under the corrected reads, compacted, and re-froze as `cohort-c2de38f09698` with the point-prevalence as-of date pinned into the snapshot rather than read from the clock. `freeze()` now refuses rows that predate the point-prevalence fields. Fixed the n-inflation arithmetic in the row above. 156 passed, 1 skipped. Four engine gates green. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Council round two: all three seats blocked again, and the headline halved.** The worst finding is one no numeric check could have caught, because every number was arithmetically correct. The draft reported "52.4% of industry date revisions were filed after the date had already passed" as non-reconciliation. **Half of those revisions, 33 of 66, set the date to ACTUAL** -- the sponsor recording when the trial finished, which for a late trial necessarily lands after the earlier estimate expired and is precisely the update 42 CFR 11.64(a)(1)(ii) requires. So the draft quoted a regulation to argue the behaviour was unlicensed while that regulation licenses half the numerator, and called the same filing "the system working" in one section and a failure in another a hundred lines apart. Verified by reconstructing the revision walk from the raw cache independently of the stored fields, which reproduced all four strata's counts exactly before the split was trusted. The surviving claim is the estimate-to-estimate subset: **33 of 126 industry revisions, 26.2%**, and 24 of the 52 industry trials that revised at all. Title, headline, table, README, SUBMISSION, COHORT and STATUS all restated on it. Second block, same shape one level up: the split conditions on a revision existing, so a trial that lapses and never files again is in neither column, and that is 32 of 60 OTHER_GOV trials. Disclosed in the section and in `docs/LIMITS.md` rather than fixed, since it is a property of the measure. Third block, from all three seats: the trial-level counts were computed in prose and were not snapshot fields, under a banner claiming every figure was snapshot-pinned and test-covered -- the same class of error as Correction 7, a guarantee that silently does not apply. Added `revisions_after_lapse_to_actual`, `revisions_after_lapse_to_estimate`, `trials_revising`, `trials_with_a_lapse`, `trials_with_lapse_to_estimate`, `trials_never_revising`, `open_estimates` and `carrying_now_of_open_rate` to `stats()`, re-measured all 240, and re-froze as `cohort-8326c1c1e964`. Also added the conditional point prevalence the target reader asked for, which is the sharper number: 5 of 15 open industry commitments are already past their date (33.3%), and **27 of 29 for OTHER_GOV (93.1%)**. Fixed the wording the hostile reviewer flagged as banned-in-substance though invisible to the regex: four attributions of belief or knowledge ("stopped believing", "knew was wrong", "honest") in a document that forbids attributing mental states, a causal "because"/"drives" asserted across four data points, and a referent switch where "it" meant a different behaviour in consecutive sentences. Disclosed the revision-level clustering (126 industry revisions from 52 trials, so no naive interval applies), the `held_days == 0` boundary, NIH median versions 106.5 printed as 106.5 rather than rounded half-to-even to 106, and the 677-day case's NCT id and source. **Found by the auditor and fixed: the retracted "n=169 was really 131" figure was still asserted as fact in `research/cohort.py` and a test docstring** -- a retracted number left standing in the source of a project whose thesis is that an uncorrected record is the defect. 156 passed, 1 skipped. Four engine gates green. **The council has not passed; a third round would be needed to confirm.** | n/a |

| 2026-07-22 | by hand (Claude Code) | **Council round three, scoped to the diff. All three seats blocked again, and two of the findings are regressions the restructure itself introduced.** Verified independently before acting. Seat 3 confirmed snapshot integrity is clean (`git diff` over `data/` empty, id still `cohort-8326c1c1e964`, 18 fields recomputed across four strata by a separate script rather than by calling `stats()`, zero mismatches) and that the round-2 citation blocks are closed. It then found three things: the 106.5 fix had been applied in one file and missed in five other places, including fourteen lines below the sentence in the same document explaining why the figure must not be rounded; the README restructure had silently dropped "at least" from "24 of the 52 trials did it at least once", making the claim arithmetically impossible against the 33-of-126 two lines above (the real distribution is 17 trials once, 6 twice, 1 four times); and both new length-bias paragraphs cite a `docs/PARKING.md` entry that was never written. All three were mine, all three are fixed, and the PARKING entry now exists. Seat 2 cleared both its round-2 blocks and found that the restructure had deleted the only small-n caution attached to the very figure it promoted ("five trials is five events"), had dropped the industry instance of its own headline (4 of the 5 industry trials currently carrying an expired estimate are invisible to the stretch measure), and that "exactly opposite order" is not exact because industry and NIH tie at 80.0%. All restored or reworded. Seat 1 found that Correction 8 retracted only half of what the bad draft published: the trial-level companion, 43 of 52 industry trials (82.7%), came from the same undivided numerator and is 24 of 52 (46.2%) under the split. A correction log that under-reports its own retraction is the failure mode the project exists to name, so both figures are now retracted explicitly. It also found the new length-bias disclosure named a bias without its direction (it is downward) or the fact that censoring is wildly unequal (0 NIH trials excluded against 27 OTHER_GOV), and that "median registry versions" counts every record edit rather than date edits: the 53x NIH/OTHER_GOV spread is 4 versus 1 on completion-date revisions alone. Same ordering, far smaller range, and the table now carries both columns. **Two framing items are returned to the owner rather than edited**: the headline asserts P(carrying | stopped filing) while the evidence supports P(stopped filing | carrying), and the two differ (of trials that never revised, only 4 of 8 industry and 0 of 5 NIH carry an expired estimate); and "indistinguishable" claims a test that was never run. Four out-of-scope contradictions in untouched text are also returned, including a sentence still calling the retracted 52.4% "stronger" than the surviving figure. No figure moved; snapshot unchanged; docs-only. 156 passed, 1 skipped. **The council has not passed.** | n/a |

| 2026-07-22 | by hand (Claude Code) | **Owner adopted the reviewer's flip: the headline conditional was backwards and is corrected in all five documents.** "The sponsors that stop filing are the ones carrying expired commitments" asserts P(carrying | stopped filing); the evidence supports P(stopped filing | carrying), and the two differ badly. Of trials that never revised a date, only 4 of 8 industry and **0 of 5 NIH** carry an expired estimate, against 4 of 5 industry carriers and 18 of 27 OTHER_GOV carriers having gone quiet. Now reads "the trials carrying expired commitments are overwhelmingly the ones whose sponsors stopped filing", with the converse-does-not-hold sentence retained so the asymmetry is stated rather than left for a reader to find. Also adopted "this study cannot separate reconciliation from filing frequency" in place of "indistinguishable", which was a term of art for a test that was never run, and reconciled the two places that contradicted it: `docs/LIMITS.md` said "the mechanism is filing frequency, and it is large" and `docs/COHORT.md` said filing frequency "drives" both, each a causal claim across four data points. Both now say the ordering follows filing frequency and is untested as a relationship. **The innocence check's argument was replaced rather than its number swapped**, which was the owner's explicit instruction and the right call. The section had claimed revision timing settled the batching question. It does not, for two independent reasons: the majority figure was the retracted 52.4%, and the surviving 26.2% estimate-to-estimate subset does not rebut batching either, because a sponsor on a yearly cycle would replace an expired estimate with a fresh one at the next pass, which is exactly what the subset counts. A cadence hypothesis is not refuted by evidence that the cadence is slow. The section now states plainly that it delivers no result, names the clustering test that would settle it, and says it has not been run. That is a weaker section and a true one. Closed the last out-of-scope item too: the banner claimed two classes of figure are not snapshot fields, and there are three, since the 159-of-188 and 32-of-48 percentile ranks for the 677-day case are computed from the store and asserted by no test. No figure moved; snapshot `cohort-8326c1c1e964` unchanged; docs-only. 156 passed, 1 skipped. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Stopped hand-editing figures and built the guard instead.** Four council rounds established the pattern: the frozen snapshot verified clean every single time, and every defect found in rounds two, three and four was in retyped prose, including one fix reported as applied in two consecutive rounds whose string replacement matched nothing and silently did nothing. The owner's call was to build the missing guard before applying another fix batch. `tests/test_prose_figures.py` is the console's number-provenance test pointed at markdown: every numeric token in a table row or bolded claim across the five claim documents must be a representation of a snapshot field or appear in an enumerated `NON_SNAPSHOT` list with its source, which is the enforced version of the write-up's "figures that are not snapshot fields" paragraph that had drifted out of date twice while it lived only in prose. It caught a real defect on its first run: the missing hedge in `docs/SUBMISSION.md`, independently found by seat 1 the same round. **Mutation-tested, and one mutation exposed a hole worth keeping.** Planting a wrong percentage is caught; deleting the hedge is caught; but reverting the median-date-changes column from 0 back to 1 PASSED, because "1" appears somewhere in the snapshot and in the regulation citation. Presence is not correspondence, which is the limit this project already records for the console's own provenance test. So the headline table's last two columns are additionally bound cell-by-cell to the fields they claim to render, by name, and restoring the off-by-one now fails. Recorded in `docs/LIMITS.md` that every other table is presence-checked only. **Fixed the off-by-one at the field level rather than in prose**, which was the owner's explicit instruction: `n_pcd_revisions` counts the initial registration, so 67 trials that never changed a date scored 1, and the published medians 4/3/2/1 credited every trial with a revision it never made. New `n_date_changes` field, correct medians 3 NIH / 2 INDUSTRY / 1 OTHER / **0 OTHER_GOV**, with a test asserting the two fields differ by exactly one and that a never-revising trial carries n_pcd_revisions == 1. The document is self-consistent again: it reports median 0 date changes for the stratum where 32 of 60 trials never revised. **Promoted the reviewer's clustering computation into the snapshot** rather than quoting it from a council transcript: `submit_intervals_*` fields with the keying convention documented in the code, giving industry n=126, median 289 days, 17 within 45 days of a year multiple (13.5%), at or below an even spread. No annual bunching. **Split the innocence-check concession** instead of conceding wholesale: batching is excluded for the currently-carrying population on duration alone, since a yearly sweep does not leave a date standing a median of 1,102 days in industry or 2,288 in OTHER_GOV, and remains formally unexcluded only for closed-spell durations. `docs/COHORT.md`'s contradicting "ruled out by the regulation" is corrected in place. **Replaced "overwhelmingly" with the measured figures and an operational definition**: no completion-date correction filed since the date lapsed, 4 of 5 industry, 20 of 27 OTHER_GOV, medians 1,102 and 2,288 days, shortest anywhere 203. Every one of those is now a snapshot field. Also restored SUBMISSION's hedge and added a cross-document consistency check for that class, fixed the banner's two-versus-three contradiction, and landed the "exactly opposite order" tie fix that had failed twice. 165 passed, 1 skipped. Four engine gates green. | n/a |

| 2026-07-22 | by hand (Claude Code) | **The guard rebuilt against the reviewers' corpus, then the definition and clustering batch run behind it.** The corpus is the seats' 19 planted defects committed as fixtures against the real documents. The first run reported 19/19 and was **vacuous**: every mutation was "caught" by a pre-existing stale snapshot id, so any mutation at all would have passed. Diffing against each document's own baseline gave the honest score, **9 of 19**. Presence cannot falsify a small integer, so the six counts the headline rests on were unfalsifiable; cell-by-cell binding of the headline table plus eight sentence-level bound claims took it to **19/19**, including "right values, wrong strata", which presence checking structurally cannot catch. Both times the corpus went stale it failed loudly rather than silently testing nothing, which is the reason to commit fixtures rather than reconstruct them. Guard amendments: snapshot-id equality scanned in raw text because ids live in code spans, N-of-M over whole files, paragraph-level scanning for continuation lines, exact-representation matching so a half-integer never prints as an integer, an audited `NON_SNAPSHOT` with a test that fails if an entry is also a real field, and a retired-phrases list. **"exactly opposite order" is now actually gone**, having survived unbolded in COHORT twice and LIMITS once across three rounds of being reported fixed. Half-integers print in full: 1,101.5 and 2,288.5 rather than 1,102 and 2,288, and OTHER's stretch p90 as 873.5. Stale ids fixed in seven documents and in `sponsor_profile.py`, two generations behind. **Then the batch.** The definition is the stronger true reading: a trial has never had a lapsed date ended by a later correction at any point in its history, not the weaker "no correction since this date lapsed", which is true of essentially every carrier by construction and measures nothing. Title says "never reconciled". The unit is stated: these sponsors kept filing other things, and `NCT05245474` filed ten registry versions while 873 days past its date. **The clustering test now publishes all four strata with the null and the ratio as snapshot fields** rather than the one stratum that supported the conclusion: industry 13.5% against a 15.3% null, ratio 0.88x, and NIH 1.51x, OTHER 1.83x, OTHER_GOV 2.13x, with OTHER's median interval of 372 days a year to within a week. The two limits are stated: the windows stop at three years while intervals run past 1,800 days, and a trial that never changed its date contributes no interval, so the carriers the section is about supply almost none of what is measured. **The innocence concession splits three ways by stratum**: the currently-carrying population excluded on duration everywhere using the population-matched field (720 days industry, not the 1,101.5 silent-subset figure an earlier draft attached to it), industry filing timing clean at 0.88x, and OTHER/OTHER_GOV closed-spell durations unexcluded at 1.83x and 2.13x. Snapshot `cohort-5b03269658b8`, unchanged: the id hashes rows and frame, so adding fields does not move it. 166 passed, 1 skipped. Four engine gates green. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Product code. The claim documents stopped being typed and started being rendered, and a seventh review round broke the guard that made that safe.** `research/render_writeup.py` renders `docs/WRITEUP.md` whole and the figure-bearing blocks of README, STATUS, COHORT, SUBMISSION and LIMITS, emitting every figure from a named snapshot field. All prose lives in a `PROSE` dict that carries no numerals, enforced over the whole module rather than over table rows and bolded lines, and the committed documents are byte-compared against a fresh render. That retires the per-figure guard, its `NON_SNAPSHOT` exception list, its representation matching and its cell bindings: a figure that is not a field can no longer be published by adding it to a list, it has to become a field. Three did. `anchor_case` (the 677-day case's days, both percentile ranks and its two dates), `clustering` (the window parameters), and the silent-carrier version distribution. **Correction 10, and it is the first defect this project found by giving an existing test a control rather than by reading its output.** The batching check counted filing intervals near one, two and three year multiples against an even-spread null. Run at control windows half a year off each anniversary, same width, the count comes back at least as high in three strata and lower by a single interval in the fourth. The null is wrong, the ratio measures a right-skewed interval distribution, and it says nothing about anniversaries. Both conclusions drawn from it are withdrawn and the innocence check now ends with one resolved leg and two unresolved, including the industry leg the owner's brief had called clean: it was read off the same retired null, and a statistic cannot be uninterpretable in three strata and interpretable in the fourth because that is the way it came out. **Correction 11** withdraws an anecdote about a trial that "filed ten registry versions while sitting past its date". `n_versions` is a lifetime count, and the timing is not merely unsupported: `dead_date_stretches == 0` is the trial's membership condition for that population and it means no filing of any kind arrived after the date passed, so the store refutes it, by construction. Checked on the trial: every version predates its registered date. Replaced by the population fact, and the correction says plainly that the generated form neither prevented this nor would have, since the error was the word "while". The definition of the measured condition now quotes `research.cohort.silent_carrier` and `carrying_expired_now` verbatim through `inspect.getsource` and cites them, instead of paraphrasing; both moved to module level so the quote is of live code. **Round seven, three seats, all three found real defects.** Seat 1 recomputed every published figure from the store by a second implementation and found no wrong cell in any table, and four wrong labels or directions in prose, including the README headline block calling OTHER_GOV's figures "government and academic sponsors", which is this project's own name for two strata whose figures differ. Seat 2 attacked the guard and demonstrated ten holes with the suite green: the corpus was **vacuous**, 22 of 23 fixtures caught by document staleness alone and 23 of 23 still "caught" with the digit rule and the residual scan deleted; a cell bound to the wrong field published; `docs/LIMITS.md` carried a prevalence table while sitting in neither guard's document list; one extra space in a block marker un-generated a block silently; `CITATIONS` re-created the exception list and passed an invented rate by construction; the quoted `research/cohort.py` source was scanned by nothing; a two-digit literal and `4 * 31` both published figures; the label exemption was read out of the module being checked, so a reviewer added a figure to it; and a code span or a link hid a retyped figure. All ten fixed, seven committed as fixtures, corpus now 30 and every generator fixture caught by a named rule rather than by staleness. Seat 3 blocked on the industry innocence leg above, on Correction 11 understating itself, and on a sentence whose figure described a different population from its own subject. Two residuals stay open and are written down: a number spelled in words is invisible to every rule here and one shipped on this pass, and a shared misunderstanding of a field is invisible to a second implementation of it. Snapshot `cohort-5b03269658b8` unchanged, store byte-identical, no existing field moved: the id hashes rows and frame, so the seven new fields do not move it. 166 passed, 1 skipped. Four engine gates green. | n/a |

| 2026-07-22 | by hand (Claude Code) | **Rounds eight to twelve: the dual-convention bounds, the jurisdiction scope, and the review run to a stopping rule.** Round eight's three seats found three defects in the round-seven published claims, all in a class no arithmetic check sees, and they became Corrections 12 to 14. A month-only registry date names no day; the engine resolves it to the first of the month, one of two readings, so every figure it touches is a bound. Both readings are snapshot fields now (`month_convention`, computed from the cache at freeze time the way `anchor_case` is), and the write-up quotes the conservative one: the anchor case is "at least 648 days", the industry mechanism rate "at least 22.2%". End-of-month is the smaller reading for the anchor and the rate but the LARGER for the closed-spell duration medians, because the shortest carries stop being lapses and drop out lifting the median; round nine caught the prose asserting it was uniformly smaller. `OTHER_GOV` as drawn is non-U.S. public bodies, not the `FED` class the study never drew, so 42 CFR Part 11's reference line is scoped to industry and NIH and the registry facts stand for the other strata without it; FED is in `docs/PARKING.md`. "Filed nothing by construction" was false and was made stronger under review: 18 of 39 silent carriers are single-version, their zero an empty loop, and one filed after its date passed; split into 21 multi-version and 18 single-version. **The guard hardened four more times.** Round seven's `check()` recomputed every table cell but nothing read its findings; round eight closed that with one assertion over the whole of `check()`, after which a wrong-field binding named in plain English fails. Everything in the snapshot outside `strata` was a trusted root until `figures_hash` covered every published block; a hand-edit to `anchor_case.days_carried` had published a fabricated headline. The block tables, the column-oriented unit table, and every table header are recomputed or pinned to independent literals. The end-of-month bound is validated by an independent `calendar.monthrange` walk rather than a re-run of the function that produced it. `freeze()` preserves an existing snapshot's `as_of` rather than reading the clock, which stopped a real drift found when the UTC date rolled over mid-session and a re-freeze walked every days-since-expiry figure forward by a day; the study date is pinned in a test because no data check can tell a right point-prevalence date from a wrong one. **The review ran to a rule, not to exhaustion.** Rounds nine, ten, eleven and twelve added no numbered correction: they found a directional sentence backwards, gaps in the guard's own coverage, a test that could corrupt what it checked, a paragraph crediting the wrong round, a commit message claiming a removal that had not landed. Each real, each fixed, none a figure or a claim a reader relies on. The write-up's methods now close with a corrections-by-layer table, rendered from a committed `CORRECTION_LAYER` record so its counts are figures: 0 in the measured-arithmetic layer, 3 in figure-production code, 5 in measure and method design, 1 in retyped prose, 5 in semantic and framing. The first row is the argument, and a test asserts it stays zero. Snapshot `cohort-5b03269658b8`, unchanged: the id hashes the measured rows, and every field added across these rounds is derived. Four engine gates green. | n/a |

| 2026-07-23 | by hand (Claude Code) | **Product code. The three-axis decision state model, which is the foundation the decision inbox is built on.** The console had one implicit notion of "state" spread across four subsystems: `Runway.reliable`, the five queue states, `promise.slip_breakdown`, and `anchor.check`. A redesign that collapsed them into one badge was specified and rejected on review, because a single red chip would have been carrying three failures that are unrelated and fail independently. `console/states.py` computes the **evidence** axis alone, from one snapshot, reading neither clock nor network: four states with the precedence written as list order rather than an if/elif ladder, so a reordering is the only way to change it. Deterministic failure outranks contingent, which is why Rocket reads `review_required` while its refused-comparison trigger sits beneath it rather than replacing it; secondary triggers are never discarded to produce the headline. The module imports nothing from `engine.ledger` or `orchestrator.anchor`, and that is enforced by a test that parses its own import graph rather than by anyone remembering. `console/review.py` owns the other two axes and joins them at request time, because both depend on files that change long after the snapshot was written. **Two defects found while building it.** `funding_threshold_approaching` was forcing `review_required`, which asserts a stated condition failed when none has: the contract still holds four months out, so it is now a `watch` trigger forcing `intact` with the trigger still listed, and the badge stops overstating the record. Separately, badge precedence and reading order had been the same ranking, which put SRPT top of the queue on `unavailable` above Rocket on a real breach: an unavailable row needs its data fixed and cannot be acted on today, so `INBOX_RANK` is now distinct from `EVIDENCE_RANK`. **Two things specified in review were not built, on purpose.** A fourth record state for "anchor unavailable" reads well and cannot be reported, because `anchor.check` folds a missing anchor into `truncated` by construction; naming it would have put a label on screen with nothing behind it. The workflow vocabulary declares six states and computes four: `in_review` and `deferred` describe a human picking a task up or pushing it out and nothing in this repo stores either fact, so they are never returned rather than synthesised from a snapshot. Resolved-detection reads the ledger **and** the review log, since `apply_decision` routes a reject to the review log and leaves the ledger untouched, and reading only the ledger would leave every rejected review sitting open forever. `_queue_rows` became a projection over the triggers instead of a second opinion about them, so the queue and the inbox cannot disagree about the same company; triggers with no legacy state are skipped rather than invented into the old counts. Verified additive: after `--displays`, the `queue`, `cmd_bar` and `unresolved` blocks are byte-identical and every contract gained only a `decision` key, nothing changed and nothing removed. 14 new tests in `tests/test_states.py`, all behaviour rather than arithmetic: tampering the ledger leaves the evidence state unmoved, a breach leaves the record state intact, and the unreliable row is still shown without a gap figure beside it. 191 passed, 1 skipped with the version cache present. Four engine gates green. (This row first read "192 passed, 1 skipped", which accounts for one more test than the suite has; corrected in the commit below that replaced the count guard.) | n/a |
| 2026-07-23 | by hand (Claude Code) | **Docs and one guard. Commit A of six, remediating an external audit.** The README's headline result, the first command a judge runs, was wrong for anyone who clones the repo. A clone carries tracked files only; `data/cache/` is gitignored; the fifteen registry-replay tests therefore skip, giving **176 passed, 16 skipped** where the README said 191 and 1. The figure was correct for a developer machine with the cache populated, which is exactly how it survived being read. What let it ship was the guard. `test_readme_test_count_matches_reality` ran `pytest --collect-only` and asserted the README's number equalled collected minus one, which is true by construction: it sees how many tests **exist** and never how many **skip**. It certified the wrong number, and then certified it again when a later commit edited 177 up to 191 without re-measuring, because 192 collected minus one is 191 whatever the suite actually does. The defect shape `docs/LIMITS.md` keeps recording, sitting on the project's flagship reproducibility claim. Replaced by `test_documented_test_counts_match_a_real_run`, which exports every tracked path at its current content into a temporary directory — what a clone yields, minus the cache — and runs the documented command there. Recursion is stopped three ways: the child deselects this test by node id, an environment marker skips it if that id ever drifts, and the git check skips it in a source download with no history. Three tiers are now documented separately and two are enforced. The clean checkout, 176 passed and 16 skipped, is **measured** on every run. The cache-present tier, 191 and 1, cannot be measured from a clean tree and is held to the same total instead. The credentialed tier quotes **no number**, because none has been measured for this commit, and inferring one is precisely what the old guard did. Six mutations watched failing before the guard was trusted: both counts of both tiers in `README.md`, and the clean-checkout count in `docs/SUBMISSION.md`, each restored green afterwards. The pair regex is whitespace-tolerant on purpose, because prose wraps and a guard a line break can switch off is the hollow kind this one exists to replace. Also states plainly that the fifteen cache-dependent tests verify the cohort research rather than the console, so nothing on the demo path depends on them. Parked rather than done: a small committed replay fixture that would let selected cache-dependent tests run in a clean clone. The full 12,043-file cache is deliberately not committed. 176 passed, 16 skipped on a clean checkout; 191 passed, 1 skipped locally. Four engine gates green. | n/a |

## Not yet started

Prompt 5, the README, is written from this file.

## What counts as Bob's work

Bob's, and describable as Bob's:
- the governance port and `engine/contract.py` (Prompt 1)
- `orchestrator/redline.py` and the fabrication test (Prompt 2)
- the console, all three views, the snapshot generator and the console tests (Prompt 3)

Not Bob's, and must be described as preceding it:
- the three `engine/` modules
- the spec, findings, port plan, demo script, prompt pack
- gate checks, verification runs and review passes, which wrote no product code

## Review passes

Not build work, and not anyone's contribution to the prototype. No product code was
written in any of these. They are listed so the build rows above can be trusted, and
because a review that found nothing is worth the same as one that found something only
if both are on the record.

The README describes how Bob was used. It does not need to enumerate these, and naming
a review tool there would answer a question nobody asked while diluting the one claim
that matters.

| Date | What |
|---|---|
| 2026-07-21 | Ran the three engine demos (all pass). Ran the Join kill gate over the full 12 name set: 8/12 joined, gate passes. Found and quantified findings 1.9 and 1.10, written into `docs/FINDINGS.md`. Created this file. |
| 2026-07-21 | By hand, not Bob. Wrote `test_display_strings_match_their_sources`, closing the drift hole found by mutation: it recomputes every display block with the real `apply_displays` and compares, so a display string cannot disagree with the field it renders. Seen failing on three separate mutations before it was kept. Replotted the panel figure as a per-trial revision timeline with runway on neither axis: the previous version was a runway-versus-move scatter, which is the causal chart for the hypothesis this project explicitly refuses to claim, and two of its three carried-expired points were the same sponsor. Confirmed `python3 console/app.py` now serves, so the entry-point item is closed. |
| 2026-07-21 | Panel verification, plus one by-hand fix. Looked at the rendered figure rather than the code: its title hardcoded "12 companies, not the sector" while its own computed caption said 7, because five of twelve tickers resolve to no live pivotal trial. Changed the title to compute the resolved count against the attempted count and regenerated. Confirmed the CSV carries 33 revision rows across 10 trials and that all three carried-expired revisions are present, including a second sponsor, ARVN at 2 days. Flagged that the figure's axes are the causal-looking scatter for a hypothesis the project explicitly refuses to claim. |
| 2026-07-21 | Real-breach verification. Confirmed the redline block is internally consistent: breach -14.5 against a [0.0, 10.4] band anchored to the +8.4 approved thesis, Granite's memo quoting the same two figures, no scripted leftovers, classification live. Mutation-tested the new lapsed-catalyst check (caught) and the command bar counts (caught, three tests). Verified both receipt hashes against the ledger file by hand. Regression: tamper demo still flips, 677 still above the fold at 1280x800. Found by mutation that display strings can drift from their source values with no test catching it, and queued the fix. |
| 2026-07-21 | Badge repair verification. Reverted the confirm handler to the query-string version and to a hardcoded `True`; the new badge test failed both times, so it is anchored to behaviour. Re-ran the four original console mutations, all still caught. Confirmed `--displays` byte-identical across runs and credential-free, the Granite block unregenerated, and the four engine gates green. Walked the tamper demo in a browser: accept, edit one byte, reload, badge flips to tampered. Noted that the provenance check matches by substring, so a truncating format passes for free. |
| 2026-07-21 | Console verification. Found the plan's display formats contradicted its own provenance test on 12 of 16 displayed figures, and amended `docs/plans/console.md` before the build reached it. Mutation-tested all four console checks (planted `9999`, renamed marker class, `stub` in the memo, `days_expired` 677 to 123); all four caught. Confirmed every display string recomputes from its source value and the Granite redline block was never regenerated. Found the confirm page reads its integrity badge from `?intact=1` rather than the ledger, so the tamper demo shows green on a corrupted file; queued the repair as Prompt 3 repair. Corrected the three misattributed rows above. |
